namespace = flavor_csh
flavor_csh.1 = {
	#大同反正
	type = country_event
	title = flavor_csh.1.title
	desc = flavor_csh.1.desc
	#historical_info = flavor_csh.1.historical_info
	trigger = {
		c:CJX ?= {
			is_subject_of = c:CSH
		}
		c:QNG ?= { has_variable = chi_domination }
		c:MNG ?= { has_variable = chi_domination }
		c:CSH ?= {
			has_variable = chi_domination
			var:chi_domination < 70			#军威
			NOT = {
				owns = location:funing
			}			#失去山海关
		}
		#
		c:WSG ?= {
			NOT = {
				is_subject_of = c:CSH				#吴三桂不是大顺附庸
			}
		}
		# OR = {
		# 	c:QNG.var:chi_domination > c:CSH.var:chi_domination			#清国军威大于大顺军威
		# 	c:MNG.var:chi_domination > c:CSH.var:chi_domination			#明国军威大于大顺军威
		# }
	}

	immediate = {
		

	}
	major = yes
	fire_only_once = yes

	dynamic_historical_event = {
		tag = CJX
		from = 1644.5.1		# The game will check if this event can be fired after this date
		to = 1700.1.1		# The game will stop checking after this date.
		monthly_chance = 100		# Monthly chance that this event will fire (100 = 100% chance)
	}

	option = {
		#降清
		name = flavor_csh.1.a
		cancel_subject = c:CJX
		historical_option = yes
		c:CJX = {
			make_subject_of = {
				target = c:QNG
				type = subject_type:cn_march_governor
			}
			# 让CJX加入大清的战争
			subject_join_overlord_wars = yes
		}
		# 增加CJX对大清的100点临时忠诚度
		c:CJX = {
			add_country_modifier = {
				modifier = loyalty_tifayifu
				years = 2
			}
		}
		c:CJX = {
			set_variable = cjx_rebellion_first
		}
		c:CSH = { trigger_event_silently = flavor_csh.2 }#通知大顺
		c:QNG = { trigger_event_silently = qng_1644_unification.9 }#通知大清
		kill_character = character:chi_zhang_tianlin
		# trigger = {
		#     c:QNG.var:chi_domination > c:CSH.var:chi_domination
		#     c:QNG.var:chi_domination > c:MNG.var:chi_domination
		# }

	}
	# option = {#归明
	#     name = flavor_csh.1.b
	#     trigger = {
	#         c:MNG.var:chi_domination > c:CSH.var:chi_domination
	#         c:MNG.var:chi_domination > c:QNG.var:chi_domination
	#     }
	#     c:CSH = {
	#         cancel_subject = c:CJX
	#     }
	#     c:CJX = {
	#         make_subject_of = {
	#             target = c:MNG
	#             type = subject_type:cn_march_governor
	#         }
	#     }
	# }
	option = {
		#忠诚 
		name = flavor_csh.1.c
		ai_chance = 0
		c:CJX = {
			add_country_modifier = {
				modifier = loyalty_tifayifu
				years = 2
			}
		}
	}
}
flavor_csh.2 = { #通知大顺
	title = flavor_csh.2.title
	desc = flavor_csh.2.desc
	is_trigger_only = yes
	option = {
		name = flavor_csh.2.a	
	}
}
